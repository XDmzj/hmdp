

# 需求

![[Pasted image 20260817114857.png]]


对于点赞这种高频变化的数据，如果我们使用MySQL是十分不理智的，因为MySQL慢、并且并发请求MySQL会影响其它重要业务，容易影响整个系统的性能，继而降低了用户体验
我们要使用Redis的Set

Set类型的数据结构具有以下特性

1. **不重复**，符合业务的特点，一个用户只能点赞一次
2. **高性能**，Set集合内部实现了高效的数据结构(Hash表)
3. **灵活性**，Set集合可以实现一对多，一个用户可以点赞多个博客，符合实际的业务逻辑

# 代码

```java
@Service
public class BlogServiceImpl extends ServiceImpl<BlogMapper, Blog> implements IBlogService {

    @Resource
    private IUserService userService;

    @Resource
    private StringRedisTemplate stringRedisTemplate;

    /**
     * 根据id查询博客
     *
     * @param id
     * @return
     */
    @Override
    public Result queryBlogById(Long id) {
        // 查询博客信息
        Blog blog = this.getById(id);
        if (Objects.isNull(blog)) {
            return Result.fail("笔记不存在");
        }
        // 查询blog相关的用户信息
        queryUserByBlog(blog);
        // 判断当前用户是否点赞该博客
        isBlogLiked(blog);
        return Result.ok(blog);
    }

    /**
     * 判断当前用户是否点赞该博客
     */
    private void isBlogLiked(Blog blog) {
        Long userId = ThreadLocalUtls.getUser().getId();
        String key = BLOG_LIKED_KEY + blog.getId();
        Boolean isMember = stringRedisTemplate.opsForSet().isMember(key, userId.toString());
        blog.setIsLike(BooleanUtil.isTrue(isMember));
    }

    /**
     * 查询热门博客
     *
     * @param current
     * @return
     */
    @Override
    public Result queryHotBlog(Integer current) {
        // 根据用户查询
        Page<Blog> page = this.query()
                .orderByDesc("liked")
                .page(new Page<>(current, SystemConstants.MAX_PAGE_SIZE));
        // 获取当前页数据
        List<Blog> records = page.getRecords();
        // 查询用户
        records.forEach(blog -> {
            this.queryUserByBlog(blog);
            this.isBlogLiked(blog);
        });
        return Result.ok(records);
    }

    /**
     * 点赞
     *
     * @param id
     * @return
     */
    @Override
    public Result likeBlog(Long id) {
        // 判断用户是否点赞
        Long userId = ThreadLocalUtls.getUser().getId();
        String key = BLOG_LIKED_KEY + blog.getId();
        // sismember key value
        Boolean isMember = stringRedisTemplate.opsForSet().isMember(key, userId.toString());
        boolean result;
        if (BooleanUtil.isFalse(isMember)) {
            // 用户未点赞，点赞数+1
            result = this.update(new LambdaUpdateWrapper<Blog>()
                    .eq(Blog::getId, id)
                    .setSql("liked = liked + 1"));
            if (result) {
                // 数据库更新成功，更新缓存  sadd key value
                stringRedisTemplate.opsForSet().add(key, userId.toString());
            }
        } else {
            // 用户已点赞，点赞数-1
            result = this.update(new LambdaUpdateWrapper<Blog>()
                    .eq(Blog::getId, id)
                    .setSql("liked = liked - 1"));
            if (result) {
                // 数据更新成功，更新缓存 srem key value
                stringRedisTemplate.opsForSet().remove(key, userId.toString());
            }
        }
        return Result.ok();
    }

    /**
     * 查询博客相关用户信息
     *
     * @param blog
     */
    private void queryUserByBlog(Blog blog) {
        Long userId = blog.getUserId();
        User user = userService.getById(userId);
        blog.setName(user.getNickName());
        blog.setIcon(user.getIcon());
    }
}

```

这段代码实现了**博客点赞功能**，核心就是利用 Redis 的 `Set` 来记录每篇博客被哪些用户点过赞，同时结合数据库完成点赞数的持久化。

---

## 一、整体结构

```
BlogServiceImpl
├── queryBlogById()        查询单篇博客（包含是否已赞状态）
├── queryHotBlog()         查询热门博客列表（按点赞数倒序）
├── likeBlog()             点赞 / 取消点赞（核心）
├── isBlogLiked()          判断当前用户是否已赞（工具方法）
└── queryUserByBlog()      填充博客作者信息（工具方法）
```

---

## 二、核心方法逐一解析

### 1. `likeBlog()`：点赞 / 取消点赞

这是最核心的方法，逻辑是**判断当前用户是否点过赞，然后执行相反操作**。

```java
public Result likeBlog(Long id) {
    Long userId = ThreadLocalUtls.getUser().getId();
    String key = BLOG_LIKED_KEY + id;   // 如：blog:liked:10086

    // ① 判断当前用户是否已点赞
    Boolean isMember = stringRedisTemplate.opsForSet()
            .isMember(key, userId.toString());

    if (BooleanUtil.isFalse(isMember)) {
        // ② 未点赞 → 点赞
        
        // 先更新数据库：点赞数 + 1
        boolean result = this.update(new LambdaUpdateWrapper<Blog>()
                .eq(Blog::getId, id)
                .setSql("liked = liked + 1"));
        
        if (result) {
            // 数据库成功，再把用户加入 Redis Set
            stringRedisTemplate.opsForSet().add(key, userId.toString());
        }
    } else {
        // ③ 已点赞 → 取消点赞
        
        // 先更新数据库：点赞数 - 1
        boolean result = this.update(new LambdaUpdateWrapper<Blog>()
                .eq(Blog::getId, id)
                .setSql("liked = liked - 1"));
        
        if (result) {
            // 数据库成功，再移除 Redis Set 中的用户
            stringRedisTemplate.opsForSet().remove(key, userId.toString());
        }
    }
    return Result.ok();
}
```

**执行流程：**

```
用户点击点赞
    │
    ▼
SISMEMBER 判断是否已赞
    │
    ├─ 未点赞：
    │    UPDATE blog SET liked = liked + 1 WHERE id = ?
    │    SADD blog:liked:{id} {userId}
    │
    └─ 已点赞：
         UPDATE blog SET liked = liked - 1 WHERE id = ?
         SREM blog:liked:{id} {userId}
```

---

### 2. `isBlogLiked()`：判断当前用户是否已赞

```java
private void isBlogLiked(Blog blog) {
    Long userId = ThreadLocalUtls.getUser().getId();
    String key = BLOG_LIKED_KEY + blog.getId();
    
    Boolean isMember = stringRedisTemplate.opsForSet()
            .isMember(key, userId.toString());
    
    blog.setIsLike(BooleanUtil.isTrue(isMember));  // 把是否已赞写入 Blog 对象
}
```

-   用 `SISMEMBER` 查 Redis
-   结果存到 `Blog` 对象的 `isLike` 字段
-   前端根据 `isLike` 显示“已赞/未赞”按钮

---

### 3. `queryBlogById()`：查单篇博客详情

```java
public Result queryBlogById(Long id) {
    // ① 数据库查博客
    Blog blog = this.getById(id);
    if (Objects.isNull(blog)) {
        return Result.fail("笔记不存在");
    }
    
    // ② 填充作者信息
    queryUserByBlog(blog);
    
    // ③ 判断当前用户是否已赞
    isBlogLiked(blog);
    
    return Result.ok(blog);
}
```

**返回给前端的数据包含：**

-   博客内容（来自数据库）
-   作者昵称和头像（来自数据库）
-   `isLike`：当前用户是否已赞（来自 Redis）

---

### 4. `queryHotBlog()`：查热门博客列表

```java
public Result queryHotBlog(Integer current) {
    // 按点赞数 liked 倒序分页查询
    Page<Blog> page = this.query()
            .orderByDesc("liked")
            .page(new Page<>(current, SystemConstants.MAX_PAGE_SIZE));
    
    List<Blog> records = page.getRecords();
    
    // 逐条填充作者信息 + 是否已赞
    records.forEach(blog -> {
        this.queryUserByBlog(blog);
        this.isBlogLiked(blog);
    });
    
    return Result.ok(records);
}
```

-   数据库查询按 `liked` 字段倒序排
-   对每一条博客，填充作者信息，再查 Redis 判断当前用户是否已赞

---

### 5. `queryUserByBlog()`：填充作者信息

```java
private void queryUserByBlog(Blog blog) {
    Long userId = blog.getUserId();
    User user = userService.getById(userId);
    blog.setName(user.getNickName());
    blog.setIcon(user.getIcon());
}
```

-   从数据库查用户
-   把昵称和头像写入 Blog 对象，前端展示用

---

## 三、Redis 和数据库如何配合

| 数据 | 存在哪 | 说明 |
|:---|:---|:---|
| 点赞总数 `liked` | 数据库 `blog` 表 | 持久化，查询热门列表时排序用 |
| 谁点过赞 | Redis Set | 快速判断当前用户是否已赞 |

**为什么这样设计？**

-   `liked` 总数放数据库：热门列表要按它排序，数据库分页排序方便
-   谁点过赞放 Redis：`SISMEMBER` 判断单用户状态极快，避免每次查数据库

---

## 四、一个潜在问题

**Redis 和数据库的一致性没有做强保证：**

```
先 UPDATE 数据库 → 成功 → 再 SADD Redis
```

如果这两步之间崩溃：

-   数据库 `liked` 已经 +1
-   但 Redis Set 里没有这个用户
-   用户下次进入页面，`SISMEMBER` 返回 false → 显示“未点赞”
-   但数据库已经加了

**改进方向**（不是本节重点）：

-   用 Lua 脚本把数据库操作和 Redis 操作做成原子
-   或引入消息队列重试
-   或直接以 Redis 为准，异步刷新数据库

---

## 五、总结

| 方法 | 作用 | 关键技术 |
|:---|:---|:---|
| `likeBlog()` | 点赞/取消点赞 | `SISMEMBER` + `SADD` + `SREM` |
| `isBlogLiked()` | 判断当前用户是否已赞 | `SISMEMBER` |
| `queryBlogById()` | 博客详情 | 数据库 + Redis 结合 |
| `queryHotBlog()` | 热门博客列表 | 数据库排序 + Redis 判断点赞状态 |
| `queryUserByBlog()` | 填充作者信息 | 数据库查询 |

**核心思想：数据库存点赞总数用于排序，Redis Set 存点赞用户用于快速判断个人点赞状态。**